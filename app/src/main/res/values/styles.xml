<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="titleTextColor">@color/darkBlue</item>
        <item name="subtitleTextColor">@color/darkBlue</item>
        <item name="android:navigationBarColor">@color/darkBlue</item>
    </style>
    <style name="MyCustomActionBarStyle" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:titleTextStyle">@font/wintercandle</item>
    </style>
</resources>
